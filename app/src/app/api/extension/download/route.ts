import { NextResponse } from 'next/server'

export async function GET() {
  // Placeholder: later copy extension-base, replace tokens, and stream a ZIP
  return NextResponse.json({ status: 'ok', message: 'ZIP generation not implemented yet' })
}
